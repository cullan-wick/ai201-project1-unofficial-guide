sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

# Ingestion pipeline
requests>=2.32
beautifulsoup4>=4.12
lxml>=5.0
markdownify>=0.13
pdfplumber==0.11.4
pyyaml>=6.0
playwright>=1.49   # headless-browser fallback for JS-rendered pages (run: playwright install chromium)

# Query interface — add whichever you use for Milestone 5:
# gradio>=6.9.0
# streamlit>=1.40.0

# Document ingestion — add if your documents/ folder contains .pdf files:
# pdfplumber==0.11.4
